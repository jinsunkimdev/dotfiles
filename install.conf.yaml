- defaults:
    link:
      create: true
      force: true
      relink: true

- clean: ['~', '~/.vim']

- link:
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc
    
- shell:
  - [git submodule update --init --recursive, Installing submodules]

